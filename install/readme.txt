阿里云的教程中针对的是thinkjs2.0，现在版本的服务器端代码已经改成了3.0，教程中其他部分不变，只是thinkjs的安装改为了npm install -g thinkjs@3，然后mysql的配置路径在src\common\config.js 30行，另外production.js的路径在项目根目录
node -v 8.4.2
npm -v 5.4.2
npm install -g cnpm
cnpm install